#include <stdio.h>
/**
 * main - print if te number is positive,zero, or negative
 *
 * description: using the main function
 * this program prints "programming is postive,zero or negative"
 * Return:0
 */
int main(void)
{
	char ch;

	for (ch = 'a' ; ch <= 'z' ; ch++)
	{
	putchar(ch);
	}	
putchar('\n');
return (0);
}
